import React from 'react';
import { Briefcase, GraduationCap, Award, Code } from 'lucide-react';

export default function Resume() {
  const experience = [
    {
      title: "Marketing Coordinator",
      company: "Tech Company",
      period: "2022 - Present",
      description: "Leading digital marketing campaigns and coordinating cross-functional teams to drive brand awareness."
    },
    {
      title: "Communications Specialist",
      company: "Business Solutions Inc.",
      period: "2020 - 2022",
      description: "Managed internal and external communications, developed content strategy, and improved stakeholder engagement."
    }
  ];

  const education = [
    {
      degree: "BA International Business Communication",
      institution: "University Name",
      period: "2016 - 2020",
      description: "Focused on cross-cultural communication, international business practices, and digital marketing."
    }
  ];

  const skills = [
    "Digital Marketing",
    "Content Strategy",
    "Project Management",
    "Business Communication",
    "Data Analysis",
    "Social Media Management"
  ];

  return (
    <div className="min-h-screen py-20 px-6">
      <div className="max-w-5xl mx-auto space-y-16">
        {/* Header */}
        <div className="text-center space-y-4">
          <h1 className="text-5xl md:text-6xl font-bold text-gray-900">Resume</h1>
          <p className="text-lg text-gray-600 max-w-2xl mx-auto">
            A summary of my professional experience, education, and skills.
          </p>
        </div>

        {/* Experience Section */}
        <section className="space-y-8">
          <div className="flex items-center gap-3">
            <div className="w-12 h-12 rounded-full bg-[#F7DF1E] flex items-center justify-center">
              <Briefcase className="w-6 h-6 text-white" />
            </div>
            <h2 className="text-3xl font-bold text-gray-900">Experience</h2>
          </div>

          <div className="grid gap-6">
            {experience.map((job, index) => (
              <div key={index} className="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow border border-gray-100">
                <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-2 mb-4">
                  <div>
                    <h3 className="text-xl font-bold text-gray-900">{job.title}</h3>
                    <p className="text-gray-600 font-medium">{job.company}</p>
                  </div>
                  <span className="text-sm text-gray-500 font-medium">{job.period}</span>
                </div>
                <p className="text-gray-600 leading-relaxed">{job.description}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Education Section */}
        <section className="space-y-8">
          <div className="flex items-center gap-3">
            <div className="w-12 h-12 rounded-full bg-[#C77DFF] flex items-center justify-center">
              <GraduationCap className="w-6 h-6 text-white" />
            </div>
            <h2 className="text-3xl font-bold text-gray-900">Education</h2>
          </div>

          <div className="grid gap-6">
            {education.map((edu, index) => (
              <div key={index} className="bg-white rounded-2xl p-8 shadow-lg hover:shadow-xl transition-shadow border border-gray-100">
                <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-2 mb-4">
                  <div>
                    <h3 className="text-xl font-bold text-gray-900">{edu.degree}</h3>
                    <p className="text-gray-600 font-medium">{edu.institution}</p>
                  </div>
                  <span className="text-sm text-gray-500 font-medium">{edu.period}</span>
                </div>
                <p className="text-gray-600 leading-relaxed">{edu.description}</p>
              </div>
            ))}
          </div>
        </section>

        {/* Skills Section */}
        <section className="space-y-8">
          <div className="flex items-center gap-3">
            <div className="w-12 h-12 rounded-full bg-[#7DD3E8] flex items-center justify-center">
              <Award className="w-6 h-6 text-white" />
            </div>
            <h2 className="text-3xl font-bold text-gray-900">Skills</h2>
          </div>

          <div className="flex flex-wrap gap-3">
            {skills.map((skill, index) => (
              <span
                key={index}
                className="px-6 py-3 bg-white rounded-full text-gray-700 font-medium shadow-md hover:shadow-lg transition-shadow border border-gray-100"
              >
                {skill}
              </span>
            ))}
          </div>
        </section>
      </div>
    </div>
  );
}